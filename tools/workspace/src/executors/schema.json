{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Deploy executor",
  "description": "",
  "type": "object",
  "properties": {
    "dockerfile": {
      "type": "string",
      "description": "The path to the dockerfile to be used."
    },
    "tag": {
      "type": "string",
      "description": "The tag to be used for the docker image."
    }
  },
  "required": ["dockerfile", "tag"]
}
