{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Deploy executor",
  "description": "",
  "type": "object",
  "properties": {
    "dockerFile": {
      "type": "string",
      "description": "Path to the Dockerfile to use for building the image.",
      "x-prompt": "What is the path to the Dockerfile?"
    },
    "tag": {
      "type": "string",
      "description": "Tag to use for the Docker build.",
      "x-prompt": "What tag should be used for the Docker build?",
      "default": "latest"
    }
  },
  "required": []
}
