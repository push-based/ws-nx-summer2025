{
  "$schema": "https://json-schema.org/schema",
  "$id": "AddDockerfile",
  "title": "",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project where the dockerfile will be added.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What project will this dockerfile be added in?",
      "x-dropdown": "projects"
    }
  },
  "required": ["project"]
}
